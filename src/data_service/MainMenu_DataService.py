import logging

logging.basicConfig(format='%(levelname)s:%(message)s', level=logging.DEBUG)


class MainMenu_DataService():
    def __init__(self):
        logging.debug("start the MainMenu_DataService()")
